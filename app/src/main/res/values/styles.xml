<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="android:panelFullBackground">@android:color/darker_gray</item>-->
        <item name="android:popupBackground">@android:color/darker_gray</item>
    </style>
    <style name="Theme.AppTheme.actionBar.widget" parent="@android:style/Theme.Holo.Light">
        <item name="android:popupBackground">@android:color/background_light</item>
    </style>

</resources>
